{
  "name": "CoClaw",
  "displayName": "CoClaw",
  "description": "AI coding assistant with persistent memory, powered by GitHub Copilot",
  "version": "0.2.2",
  "publisher": "gdhanush270",
  "license": "Apache-2.0",
  "icon": "icon.png",
  "galleryBanner": {
    "color": "#0a0a1a",
    "theme": "dark"
  },
  "keywords": [
    "copilot",
    "ai",
    "memory",
    "assistant",
    "chat",
    "agent"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/gdhanush27/Co-Claw"
  },
  "engines": {
    "vscode": "^1.93.0"
  },
  "extensionDependencies": [
    "github.copilot-chat"
  ],
  "categories": [
    "AI",
    "Chat"
  ],
  "activationEvents": [],
  "main": "./dist/extension.js",
  "contributes": {
    "chatParticipants": [
      {
        "id": "CoClaw.assistant",
        "name": "CoClaw",
        "fullName": "CoClaw",
        "description": "Your AI pair programmer with persistent memory",
        "isSticky": true,
        "commands": [
          {
            "name": "memory",
            "description": "Show what CoClaw remembers"
          },
          {
            "name": "distill",
            "description": "Distill recent logs into long-term memory"
          },
          {
            "name": "clear",
            "description": "Clear session memory"
          },
          {
            "name": "soul",
            "description": "Edit CoClaw's identity and behavior"
          },
          {
            "name": "auto",
            "description": "Start Telegram bridge — control CoClaw from Telegram with full tool access"
          },
          {
            "name": "open",
            "description": "Start OpenClaw mode — Telegram bridge + Heartbeat + workspace memory (full autonomous agent)"
          },
          {
            "name": "agents",
            "description": "Run a multi-agent orchestration: Planner decomposes the task, Coder/Reviewer/Tester/Memory agents execute in parallel"
          }
        ]
      }
    ],
    "languageModelTools": [
      {
        "name": "CoClaw_memory_read",
        "displayName": "Read Memory",
        "modelDescription": "Reads stored memory entries from CoClaw's memory store",
        "inputSchema": {
          "type": "object",
          "properties": {
            "query": {
              "type": "string",
              "description": "Keyword to search for in memory"
            },
            "layer": {
              "type": "string",
              "enum": [
                "daily",
                "longterm",
                "all"
              ],
              "description": "Which memory layer to search"
            }
          }
        }
      },
      {
        "name": "CoClaw_memory_write",
        "displayName": "Write Memory",
        "modelDescription": "Saves a new fact or preference to CoClaw's long-term memory",
        "inputSchema": {
          "type": "object",
          "required": [
            "content",
            "type"
          ],
          "properties": {
            "content": {
              "type": "string",
              "description": "The memory content to store"
            },
            "type": {
              "type": "string",
              "enum": [
                "fact",
                "preference",
                "decision",
                "code_context"
              ],
              "description": "Type of memory entry"
            },
            "importance": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "Importance weight (0-1)"
            }
          }
        }
      },
      {
        "name": "CoClaw_memory_delete",
        "displayName": "Delete Memory",
        "modelDescription": "Deletes memory entries from CoClaw's memory store. Can delete by ID, search query, or clear an entire layer. Pass layer='all' without id/query to wipe everything. Pass layer='longterm' or layer='daily' to clear that layer. Pass query to search-and-delete individual entries.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "The exact ID of the memory entry to delete"
            },
            "query": {
              "type": "string",
              "description": "Search query to find the memory to delete (used when id is not known)"
            },
            "layer": {
              "type": "string",
              "enum": [
                "daily",
                "longterm",
                "all"
              ],
              "description": "Memory layer to target. When used without id/query, clears all entries in that layer."
            }
          }
        }
      },
      {
        "name": "CoClaw_memory_update",
        "displayName": "Update Memory",
        "modelDescription": "Updates an existing memory entry's content, type, or importance. Use CoClaw_memory_read first to find the entry, then update by id.",
        "inputSchema": {
          "type": "object",
          "properties": {
            "id": {
              "type": "string",
              "description": "The exact ID of the memory entry to update"
            },
            "query": {
              "type": "string",
              "description": "Search query to find the memory to update (used when id is not known)"
            },
            "content": {
              "type": "string",
              "description": "New content for the memory entry"
            },
            "type": {
              "type": "string",
              "enum": [
                "fact",
                "preference",
                "decision",
                "code_context",
                "convention",
                "pattern"
              ],
              "description": "New type for the memory entry"
            },
            "importance": {
              "type": "number",
              "minimum": 0,
              "maximum": 1,
              "description": "New importance weight (0-1)"
            }
          }
        }
      },
      {
        "name": "CoClaw_workspace_context",
        "displayName": "Get Workspace Context",
        "modelDescription": "Gets the active file, selection, and open tabs in VS Code",
        "inputSchema": {
          "type": "object",
          "properties": {}
        }
      },
      {
        "name": "CoClaw_shared_memory_read",
        "displayName": "Read Shared Memory",
        "modelDescription": "Read shared key/value entries written by sibling agents in the current multi-agent run. Filter by runId and optionally a specific key.",
        "inputSchema": {
          "type": "object",
          "required": ["runId"],
          "properties": {
            "runId": {"type": "string", "description": "The orchestration run id (provided in your system prompt)"},
            "key": {"type": "string", "description": "Optional specific key to fetch"}
          }
        }
      },
      {
        "name": "CoClaw_shared_memory_write",
        "displayName": "Write Shared Memory",
        "modelDescription": "Write a key/value entry to the shared memory store for the current multi-agent run so dependent agents can read it.",
        "inputSchema": {
          "type": "object",
          "required": ["runId", "key", "value"],
          "properties": {
            "runId": {"type": "string", "description": "The orchestration run id"},
            "key": {"type": "string", "description": "Identifier such as 'coder:task-1' or 'review:summary'"},
            "value": {"type": "string", "description": "The string value to store (JSON-encode complex data)"},
            "writtenBy": {"type": "string", "enum": ["planner", "coder", "reviewer", "tester", "memory", "orchestrator"], "description": "Your agent role"}
          }
        }
      },
      {
        "name": "CoClaw_get_task_status",
        "displayName": "Get Agent Task Status",
        "modelDescription": "Inspect the live status of all tasks in a multi-agent run, or a specific task. Returns pending/running/done/failed and dependency info.",
        "inputSchema": {
          "type": "object",
          "required": ["runId"],
          "properties": {
            "runId": {"type": "string", "description": "The orchestration run id"},
            "taskId": {"type": "string", "description": "Optional specific task id"}
          }
        }
      },
      {
        "name": "CoClaw_spawn_agent",
        "displayName": "Spawn Agent",
        "modelDescription": "Spawn a new specialized agent task within the current run. ORCHESTRATOR USE ONLY — other agents do not have access. Resolves once the spawned task completes.",
        "inputSchema": {
          "type": "object",
          "required": ["runId", "agent", "prompt"],
          "properties": {
            "runId": {"type": "string"},
            "agent": {"type": "string", "enum": ["coder", "reviewer", "tester", "memory"]},
            "prompt": {"type": "string"},
            "dependsOn": {"type": "array", "items": {"type": "string"}, "description": "Optional task ids this dynamic task should wait for"}
          }
        }
      },
      {
        "name": "CoClaw_telegram_send_file",
        "displayName": "Send File via Telegram",
        "modelDescription": "Upload a file from the workspace to the linked Telegram user as a document. Use when the user asks you to send/share/email/upload a file over Telegram. Path must be inside the workspace folder. Max 50 MB.",
        "inputSchema": {
          "type": "object",
          "required": ["path"],
          "properties": {
            "path": {"type": "string", "description": "Workspace-relative file path (preferred) or absolute path inside the workspace."},
            "caption": {"type": "string", "description": "Optional caption (HTML allowed, ~1000 char max)."}
          }
        }
      }
    ],
    "commands": [
      {
        "command": "CoClaw.selectModel",
        "title": "CoClaw: Select Model"
      },
      {
        "command": "CoClaw.showMemory",
        "title": "CoClaw: Browse Memory"
      },
      {
        "command": "CoClaw.clearMemory",
        "title": "CoClaw: Clear Session Memory"
      },
      {
        "command": "CoClaw.editSoul",
        "title": "CoClaw: Edit Identity (SOUL)"
      },
      {
        "command": "CoClaw.editProfile",
        "title": "CoClaw: Edit Profile (USER)"
      },
      {
        "command": "CoClaw.distill",
        "title": "CoClaw: Distill to Long-Term Memory"
      },
      {
        "command": "CoClaw.importMemories",
        "title": "CoClaw: Import Memories"
      },
      {
        "command": "CoClaw.exportMemories",
        "title": "CoClaw: Export Memories"
      },
      {
        "command": "CoClaw.deduplicateMemory",
        "title": "CoClaw: Deduplicate Memory"
      },
      {
        "command": "CoClaw.stop",
        "title": "CoClaw: Stop Response",
        "icon": "$(debug-stop)"
      },
      {
        "command": "CoClaw.linkTelegram",
        "title": "CoClaw: Link Telegram Bot"
      },
      {
        "command": "CoClaw.unlinkTelegram",
        "title": "CoClaw: Unlink Telegram Bot"
      },
      {
        "command": "CoClaw.clearCronJobs",
        "title": "CoClaw: Clear All Cron Jobs"
      },
      {
        "command": "CoClaw.openCronStorage",
        "title": "CoClaw: Open Cron Storage"
      },
      {
        "command": "CoClaw.openSettings",
        "title": "CoClaw: Open Settings"
      }
    ],
    "walkthroughs": [
      {
        "id": "CoClaw.welcome",
        "title": "Get Started with CoClaw",
        "description": "Learn how CoClaw adds persistent memory to GitHub Copilot.",
        "steps": [
          {
            "id": "CoClaw.welcome.intro",
            "title": "What is CoClaw?",
            "description": "CoClaw enhances GitHub Copilot with a persistent memory layer. It remembers your code conventions, preferences, and project decisions across sessions — and injects them into every LLM prompt automatically.\n\n[Open Chat](command:workbench.action.chat.open)",
            "media": {
              "altText": "CoClaw overview",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.profile",
            "title": "Set Up Your Profile",
            "description": "Tell CoClaw about your coding preferences — language, style, indentation, frameworks. This helps it tailor responses to your workflow.\n\n[Edit USER.json](command:CoClaw.editProfile)",
            "media": {
              "altText": "User profile setup",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.soul",
            "title": "Customize the Assistant Persona",
            "description": "CoClaw's personality is defined in SOUL.json. Change its name, role, tone, or instructions to match your preferences.\n\n[Edit SOUL.json](command:CoClaw.editSoul)",
            "media": {
              "altText": "Soul configuration",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.usage",
            "title": "Start a Conversation",
            "description": "Type **@CoClaw** in the GitHub Copilot Chat panel to start a memory-augmented conversation. Use slash commands:\n- `/memory` — See what CoClaw remembers\n- `/distill` — Save daily facts to long-term memory\n- `/clear` — Clear today's session log\n- `/soul` — Edit the assistant persona\n\n[Open Chat](command:workbench.action.chat.open)",
            "media": {
              "altText": "Using CoClaw in chat",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.memory",
            "title": "Browse & Manage Memories",
            "description": "Open the Memory Browser to see all stored entries, filter by type, promote daily logs to long-term memory, or delete entries.\n\n[Open Memory Browser](command:CoClaw.showMemory)",
            "media": {
              "altText": "Memory browser",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.agents",
            "title": "Run a Multi-Agent Plan",
            "description": "Type **@CoClaw /agents <task>** in the chat panel to break a request into a Planner → Coder(s) → Reviewer → Tester pipeline that runs through shared memory. Use the CoClaw activity-bar icon to watch the live agent tree, and tune `CoClaw.agents.mode` if you want every prompt to route through the orchestrator.\n\n[Open Chat](command:workbench.action.chat.open)",
            "media": {
              "altText": "Multi-agent orchestration",
              "image": "images/icon.png"
            }
          },
          {
            "id": "CoClaw.welcome.telegram",
            "title": "Link Telegram for Remote Control",
            "description": "Run **CoClaw: Link Telegram Bot**, paste a token from @BotFather plus your numeric user ID (from @userinfobot), then send **@CoClaw /auto** in chat to start the bridge — or **@CoClaw /open** for OpenClaw mode (heartbeat checks, cron jobs, file delivery).\n\n[Link Telegram Bot](command:CoClaw.linkTelegram)",
            "media": {
              "altText": "Telegram bridge setup",
              "image": "images/icon.png"
            }
          }
        ]
      }
    ],
    "configuration": {
      "title": "CoClaw",
      "properties": {
        "CoClaw.model.family": {
          "type": "string",
          "description": "Preferred Copilot model family (workspace override)",
          "default": ""
        },
        "CoClaw.memory.maxLongTermEntries": {
          "type": "number",
          "default": 100,
          "description": "Maximum number of long-term memory entries"
        },
        "CoClaw.memory.dailyLogsRetentionDays": {
          "type": "number",
          "default": 30,
          "description": "Number of days to retain daily logs"
        },
        "CoClaw.memory.autoExtract": {
          "type": "boolean",
          "default": true,
          "description": "Automatically extract facts from responses into daily log"
        },
        "CoClaw.memory.tokenBudgetPercent": {
          "type": "number",
          "default": 20,
          "description": "Max % of model context window to use for memory injection"
        },
        "CoClaw.memory.autoDistillThreshold": {
          "type": "number",
          "default": 20,
          "description": "Auto-distill when daily log reaches this many entries (0 to disable)"
        },
        "CoClaw.memory.autoDistillIntervalHours": {
          "type": "number",
          "default": 24,
          "description": "Auto-distill interval in hours (0 to disable scheduled distill)"
        },
        "CoClaw.memory.staleAfterDays": {
          "type": "number",
          "default": 14,
          "description": "Long-term entries not referenced in this many days are downgraded in recall ranking (0 to disable)"
        },
        "CoClaw.heartbeat.enabled": {
          "type": "boolean",
          "default": true,
          "description": "Enable heartbeat (periodic autonomous agent checks) in /open mode"
        },
        "CoClaw.heartbeat.intervalMinutes": {
          "type": "number",
          "default": 30,
          "description": "Heartbeat interval in minutes (how often the agent checks HEARTBEAT.md)"
        },
        "CoClaw.heartbeat.activeHoursStart": {
          "type": "string",
          "default": "08:00",
          "description": "Start of active hours for heartbeat (HH:MM, 24h format)"
        },
        "CoClaw.heartbeat.activeHoursEnd": {
          "type": "string",
          "default": "22:00",
          "description": "End of active hours for heartbeat (HH:MM, 24h format)"
        },
        "CoClaw.heartbeat.timezone": {
          "type": "string",
          "default": "",
          "description": "IANA time zone (e.g. 'Asia/Kolkata', 'America/New_York') used to evaluate heartbeat active hours. Leave empty to use the host's local time."
        },
        "CoClaw.logging.level": {
          "type": "string",
          "enum": ["off", "error", "warn", "info", "debug"],
          "default": "error",
          "description": "Verbosity for the CoClaw output channel (View → Output → CoClaw). Use 'debug' to capture full traces when filing a bug."
        },
        "CoClaw.telegram.sarcasticReactions": {
          "type": "boolean",
          "default": true,
          "description": "In /open mode, react to each user message with a sarcastic emoji."
        },
        "CoClaw.telegram.tone": {
          "type": "string",
          "enum": ["sarcastic", "friendly", "professional", "playful", "neutral"],
          "default": "sarcastic",
          "description": "Conversational tone the assistant uses in Telegram /open mode."
        },
        "CoClaw.telegram.silentUnauthorized": {
          "type": "boolean",
          "default": false,
          "description": "Silently ignore messages from non-linked Telegram users instead of replying with an 'Unauthorized' notice. Useful when the bot is added to shared chats and you don't want to advertise its presence."
        },
        "CoClaw.telegram.useEmojis": {
          "type": "boolean",
          "default": true,
          "description": "Whether the assistant may use emojis in Telegram /open mode (and react to incoming messages)."
        },
        "CoClaw.agents.mode": {
          "type": "string",
          "enum": ["off", "slash", "always"],
          "default": "slash",
          "description": "Multi-agent orchestration mode. 'slash' = run only on /agents. 'always' = route every prompt through the orchestrator (experimental). 'off' = disabled."
        },
        "CoClaw.agents.maxParallelCoders": {
          "type": "integer",
          "default": 4,
          "minimum": 1,
          "maximum": 8,
          "description": "Maximum number of parallel Coder agents the orchestrator may fan out for a single coder task."
        },
        "CoClaw.tools.maxPerRequest": {
          "type": "integer",
          "default": 120,
          "minimum": 1,
          "maximum": 256,
          "description": "Maximum number of tools to send in a single language-model request. Most providers (OpenAI, Gemini) reject requests with more than 128 tools. The default of 120 leaves headroom; lower it if you still see 'Cannot have more than N tools per request' errors. Tools are kept in this priority order: user-prioritized → CoClaw_* → core file/edit/search tools → others (alphabetic)."
        },
        "CoClaw.tools.exclude": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Case-insensitive substring patterns; any tool whose name contains a listed pattern is dropped before the request reaches the model. Useful for noisy MCP servers or extension tools you never want the assistant to call (e.g. ['mssql', 'jupyter'])."
        },
        "CoClaw.tools.priority": {
          "type": "array",
          "items": { "type": "string" },
          "default": [],
          "description": "Case-insensitive substring patterns for tools that must survive the per-request cap. Bumps matching tools above CoClaw's own tools in the selection order — use sparingly (e.g. ['github_pr', 'k8s_apply'] to guarantee a niche MCP tool stays available even when 200+ tools are registered)."
        }
      }
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "CoClawAgents",
          "title": "CoClaw Agents",
          "icon": "icon.png"
        }
      ]
    },
    "views": {
      "CoClawAgents": [
        {
          "id": "CoClaw.agentsView",
          "name": "Agents"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run build",
    "build": "node esbuild.js --production",
    "watch": "node esbuild.js --watch",
    "lint": "eslint src --ext ts",
    "typecheck": "tsc --noEmit",
    "test": "mocha --require ts-node/register --require src/test/setup.ts src/test/**/*.test.ts --timeout 10000",
    "package": "vsce package --allow-missing-repository"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^20.11.0",
    "@types/vscode": "^1.93.0",
    "@vscode/vsce": "^3.0.0",
    "esbuild": "^0.25.0",
    "mocha": "^11.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.3.0"
  }
}
